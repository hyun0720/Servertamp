<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 테두리 내부 색상 설정 -->
    <solid android:color="#f2e8de" />

    <!-- 테두리 내부 여백 설정 -->
    <padding
        android:bottom="50dp"
        android:left="40dp"
        android:right="40dp"
        android:top="50dp" />

    <!-- 테두리 설정: 두께, 색상 -->
    <stroke
        android:width="2dp"
        android:color="#000000" />

</shape>

